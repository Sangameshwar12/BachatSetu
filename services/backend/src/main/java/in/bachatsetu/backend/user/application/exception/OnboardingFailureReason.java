package in.bachatsetu.backend.user.application.exception;

/** Reasons a profile onboarding attempt can be rejected. */
public enum OnboardingFailureReason {
    PROFILE_NOT_FOUND,
    ALREADY_ONBOARDED
}
