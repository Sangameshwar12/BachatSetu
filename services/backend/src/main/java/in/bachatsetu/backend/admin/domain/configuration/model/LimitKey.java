package in.bachatsetu.backend.admin.domain.configuration.model;

/** The platform-wide system limits that can be independently configured. */
public enum LimitKey {
    MAX_GROUPS,
    MAX_MEMBERS,
    MAX_UPLOADS,
    MAX_RECEIPTS,
    MAX_NOTIFICATIONS
}
