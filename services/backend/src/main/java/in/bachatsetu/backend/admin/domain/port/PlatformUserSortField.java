package in.bachatsetu.backend.admin.domain.port;

/** Fields a platform user search may be sorted by. */
public enum PlatformUserSortField {
    CREATED_AT,
    FIRST_NAME,
    LAST_NAME,
    EMAIL
}
