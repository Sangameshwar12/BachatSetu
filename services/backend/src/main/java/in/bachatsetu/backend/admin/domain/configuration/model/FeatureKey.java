package in.bachatsetu.backend.admin.domain.configuration.model;

/** The platform-wide features that can be independently enabled or disabled. */
public enum FeatureKey {
    AUTHENTICATION,
    PAYMENTS,
    NOTIFICATIONS,
    STORAGE,
    RECEIPTS,
    AUCTION,
    ANALYTICS,
    AUDIT,
    SIGNUP
}
