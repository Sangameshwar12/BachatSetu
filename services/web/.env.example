# Base URL of the BachatSetu backend API. Must be reachable from the browser (CORS-enabled),
# not just from the server — every request is made client-side via axios.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
